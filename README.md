# simple-boilerplate
A simple set, for a quick start. Try the new features of JavaScript with Babel.

- Babel
- PostCSS
- ESlint

```yarn install```  
```yarn run dev```  
```yarn run build```  
```yarn run fix```  
